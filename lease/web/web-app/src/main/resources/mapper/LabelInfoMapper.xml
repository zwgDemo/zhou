<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.app.mapper.LabelInfoMapper">


    <select id="selectLabelInfoById" resultType="com.atguigu.lease.model.entity.LabelInfo">
        select id, type, name
        from label_info li
        where li.is_deleted = 0
          and li.id in (select label_id
                       from apartment_label al
                       where al.is_deleted = 0
                         and al.apartment_id = #{apartmentId})

    </select>

    <select id="selectLabelInfoByRoomId" resultType="com.atguigu.lease.model.entity.LabelInfo">
        select id, type, name, icon
        from facility_info fi
        where fi.is_deleted = 0
          and fi.id in (select facility_id
                       from room_facility rf
                       where rf.is_deleted = 0
                         and rf.room_id = #{id})
    </select>
</mapper>
