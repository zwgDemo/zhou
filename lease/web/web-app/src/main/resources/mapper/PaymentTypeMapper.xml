<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.app.mapper.PaymentTypeMapper">


    <select id="selectPaymentTypeByRoomId" resultType="com.atguigu.lease.model.entity.PaymentType">
        select id, name, pay_month_count, additional_info
        from payment_type pt
        where pt.is_deleted = 0
          and pt.id in (select payment_type_id
                       from room_payment_type rpt
                       where rpt.is_deleted = 0
                         and rpt.room_id = #{id})
    </select>
</mapper>